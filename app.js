document.documentElement.classList.remove('no-js');
const menu=document.querySelector('.menu-button');
const nav=document.querySelector('.nav');
if(menu&&nav){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false');}));}
document.querySelectorAll('[data-contact-form]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const status=form.querySelector('.form-status');const data=new FormData(form);const required=[...form.querySelectorAll('[required]')];if(required.some(el=>el.type==='checkbox'?!el.checked:!el.value.trim())){status.textContent='必須項目をご確認ください。';status.style.display='block';return;}const subject=`【Malz Insight】Waitlist登録 - ${data.get('company')}`;const body=[`会社名：${data.get('company')}`,`お名前：${data.get('name')}`,`メール：${data.get('email')}`,`お立場：${data.get('topic')||''}`,`関心テーマ・補足：${data.get('message')||''}`].join('\n');status.textContent='メール作成画面を開きます。内容をご確認のうえ送信してください。';status.style.display='block';window.location.href=`mailto:ttakino@cosmosketch.co.jp?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;}));
